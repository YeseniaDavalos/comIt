function mostrarValor() {
    // Intentamos imprimir la variable valor antes de declararla
    console.log(valor);
    // Declaramos la variable valor con let
    let valor = 5;
}

  // Llamada a la función mostrarValor
smostrarValor();