// Declarar la variable nota y asignar un valor entre 0 y 10
let nota = 8;

// Verificar la nota y mostrar el mensaje correspondiente
if (nota >= 9) {
console.log('El alumno aprobó y es muy bueno');
} else if (nota < 9 && nota >= 6) {
console.log('El alumno aprobó');
} else {
console.log('El alumno no aprobó y debe hacer los ejercicios de nuevo');
}

// Cambiar el valor de la variable nota un par de veces y ejecutar para ver los diferentes resultados
nota = 5;
if (nota >= 9) {
console.log('El alumno aprobó y es muy bueno');
} else if (nota < 9 && nota >= 6) {
console.log('El alumno aprobó');
} else {
console.log('El alumno no aprobó y debe hacer los ejercicios de nuevo');
}
