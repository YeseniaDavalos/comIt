// Declarar las variables
let numero1 = 10;
let numero2 = 5;

// Comparar las variables y mostrar el mensaje correspondiente
if (numero1 > numero2) {
    console.log('La variable numero1 es más grande que la variable numero2');
} else {
    console.log('La variable numero2 es más grande que la variable numero1');
}

// Probar cambiando el valor de numero2
numero2 = 15;

// Volver a comparar y mostrar el mensaje
if (numero1 > numero2) {
    console.log('La variable numero1 es más grande que la variable numero2');
} else {
    console.log('La variable numero2 es más grande que la variable numero1');
}
